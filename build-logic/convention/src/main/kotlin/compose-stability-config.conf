// Compose compiler stability configuration.
// Types listed here are treated as stable even though they live in modules that do not
// have the Compose compiler on their classpath (:domain is a pure-Kotlin module;
// kotlinx-datetime ships no Compose annotations).
//
// Format: one fully-qualified class name per line.
// Wildcards are supported: com.example.* covers all direct members of that package.
// See: https://developer.android.com/develop/ui/compose/performance/stability/fix#configuration-file

// ── :domain ──────────────────────────────────────────────────────────────────
com.datecountdown.app.domain.Event
com.datecountdown.app.domain.EventId
// Enums are stable by default; listed here for explicit documentation.
com.datecountdown.app.domain.EventColor
com.datecountdown.app.domain.EventIcon
com.datecountdown.app.domain.CountdownUnit
com.datecountdown.app.domain.CountdownResult
com.datecountdown.app.domain.CountdownResult.Upcoming
com.datecountdown.app.domain.CountdownResult.Past
com.datecountdown.app.domain.PastBreakdown
com.datecountdown.app.domain.PastBreakdown.Today
com.datecountdown.app.domain.PastBreakdown.DaysAgo

// ── kotlinx-datetime ─────────────────────────────────────────────────────────
kotlinx.datetime.Instant
kotlinx.datetime.LocalDateTime
